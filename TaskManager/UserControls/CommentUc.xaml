<userControls:ListedUc x:TypeArguments="models:Comment"
                       x:Class="TaskManager.UserControls.CommentUc"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:userControls="clr-namespace:TaskManager.UserControls"
                       xmlns:models="clr-namespace:TaskManager.DB.Models;assembly=TaskManager.DB"
                       mc:Ignorable="d">
    <!--делает ламер-->
    <Grid>
        <Label x:Name="CreatorLogin"
               Content="CreatorLogin"
               HorizontalAlignment="Left"
               Margin="10,10,0,0"
               VerticalAlignment="Top" />
        <Label x:Name="CreationTime"
               Content="CreationTime"
               HorizontalAlignment="Left"
               Margin="140,10,0,0"
               VerticalAlignment="Top" />
        <TextBlock x:Name="Description"
                   HorizontalAlignment="Left"
                   Margin="10,41,0,0"
                   TextWrapping="Wrap"
                   Text="Description"
                   VerticalAlignment="Top"
                   Height="54"
                   Width="508" />
    </Grid>
</userControls:ListedUc>
